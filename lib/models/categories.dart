//For future method of all the rows in games page
import 'package:contenterra_assignment/each_category.dart';

class Categories {
  int categoryId;
  String mainTitle;
  EachCategory item;

  Categories({
    required this.categoryId,
    required this.mainTitle,
    required this.item,
  });
}